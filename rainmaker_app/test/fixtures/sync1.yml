address: localhost
local_root: ${abs_path:${user_dir}/../sync1}
port: '22'
remote_root: ${abs_path:${user_dir}/../sync2}
do_ignores: false
do_backups: false
ssh_key_path: /home/ubuntu/.ssh/rain
title: sync1
type: unison
user: ubuntu
guid: sync1_guid
