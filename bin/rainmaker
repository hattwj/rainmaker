#!/usr/bin/env python3
import sys
import rainmaker.main
import rainmaker.cli
import rainmaker.tasks

def main():
    config = rainmaker.cli.parse()
    app = rainmaker.main.Application(**config) 
    rainmaker.tasks.autorun(app)

if __name__ == '__main__':
    sys.exit(main())
